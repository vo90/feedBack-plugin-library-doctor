# FeedBack provides FastAPI and PyYAML. Its plugin loader installs these into
# the selected profile's pip_packages directory, never the system environment.
jsonschema>=4.18,<5
construct==2.10.70
cryptography==50.0.0
